---
sidebar_position: 2
sidebar_label: GPS Navigation
---
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import ReactPlayer from 'react-player';

# GPS Navigation

We can run an outdoor demonstration of MAVROSPY via the [`outdoor.launch`](https://github.com/bandofpv/mavrospy/blob/main/launch/outdoor.launch) file. However, we need to make sure it runs on boot as we would not be able to SSH to our companion computer on the field.

To do this, we must ensure that Docker starts on boot:

```bash
$ sudo systemctl enable docker
```

Reboot the companion computer and check the status of `docker`:

```bash
$ sudo systemctl status docker
```

You should see `Active: active (running)` somewhere in the status report. **CTRL+C** to exit.

Docker provides restart policies to start containers on boot. We can take advantage of this by utilzing a layer on top of our MAVROSPY image which will run the demo launch file upon startup.

<Tabs groupId="companion-computers">
  <TabItem value="rpi" label="RPi4">
    Build via the `run_docker.sh` script:

    ```bash
    $ cd ~/mavrospy/docker/rpi/outdoor
    $ bash run_docker.sh
    ```
  </TabItem>

  <TabItem value="jetson-nano" label="Jetson Nano">
    Build via the `run_docker.sh` script:

    ```bash
    $ cd ~/mavrospy/docker/jetson/outdoor
    $ bash run_docker.sh
    ```
  </TabItem>

  <TabItem value="jetson-orin" label="Jetson Orin Nano">
    Build via the `run_docker.sh` script:

    ```bash
    $ cd ~/mavrospy/docker/jetson/outdoor
    $ bash run_docker.sh
    ```
  </TabItem>
</Tabs>

The script will automatically start building the outdoor docker image and run it in a container. Notice how it immediatly launches the outdoor demo.

We want to keep this container running so don't close it. Power off your companion computer.

<details>

<summary>Specify Flight Pattern</summary>

You can specify the flight pattern to any of the available patterns: `square`, `square_head`, `circle`, `circle_head`, `figure8`, `figure8_head`, `spiral`, and `spiral_head`. Where `_head` will specify the drone to face in the direction of motion. 

However, doing so will require additional steps:

<Tabs groupId="companion-computers">
  <TabItem value="rpi-ros2" label="RPi4 (ROS2)">
    Remove the outdoor MAVROSPY container and image:

    ```bash
    $ docker container rm -f mavrospy2-rpi-outdoor-container
    $ docker image rm mavrospy2-rpi-outdoor
    ```

    Then modify the outdoor MAVROSPY Dockerfile:

    ```bash
    $ cd ~/mavrospy/docker/rpi/outdoor
    $ nano Dockerfile
    ```

    The last line should look like this:

    ```bash
    ENTRYPOINT ["/bin/bash", "-c", "source /opt/ros/humble/setup.bash && source /home/rpi/ros2_ws/install/setup.bash && sudo chown rpi /dev/serial0 && ros2 launch mavrospy outdoor.py"]
    ```

    Modify the last part to specify the `pattern`:

    ```bash
    ENTRYPOINT ["/bin/bash", "-c", "source /opt/ros/humble/setup.bash && source /home/rpi/ros2_ws/install/setup.bash && sudo chown rpi /dev/serial0 && ros2 launch mavrospy outdoor.py pattern:=circle"]
    ```

    Rebuild via the `run_docker.sh` script:

    ```bash
    $ cd ~/mavrospy/docker/rpi/outdoor
    $ bash run_docker.sh
    ```
  </TabItem>

  <TabItem value="rpi-ros1" label="RPi4 (ROS1)">
    Remove the outdoor MAVROSPY container and image:

    ```bash
    $ docker container rm -f mavrospy-rpi-outdoor-container
    $ docker image rm mavrospy-rpi-outdoor
    ```

    Then modify the outdoor MAVROSPY Dockerfile:

    ```bash
    $ cd ~/mavrospy/docker/rpi/outdoor
    $ nano Dockerfile
    ```

    The last line should look like this:

    ```bash
    ENTRYPOINT ["/bin/bash", "-c", "source /opt/ros/noetic/setup.bash && source /home/rpi/catkin_ws/devel/setup.bash && sudo chown rpi /dev/serial0 && roslaunch mavrospy outdoor.launch"]
    ```

    Modify the last part to specify the `pattern`:

    ```bash
    ENTRYPOINT ["/bin/bash", "-c", "source /opt/ros/noetic/setup.bash && source /home/rpi/catkin_ws/devel/setup.bash && sudo chown rpi /dev/serial0 && roslaunch mavrospy outdoor.launch pattern:=circle"]
    ```

    Rebuild via the `run_docker.sh` script:

    ```bash
    $ cd ~/mavrospy/docker/rpi/outdoor
    $ bash run_docker.sh
    ```
  </TabItem>

  <TabItem value="jetson-ros2" label="Jetson Nano (ROS2)">
    Remove the outdoor MAVROSPY container and image:

    ```bash
    $ docker container rm -f mavrospy2-jetson-outdoor-container
    $ docker image rm mavrospy2-jetson-outdoor
    ```

    Then modify the outdoor MAVROSPY Dockerfile:

    ```bash
    $ cd ~/mavrospy/docker/jetson/outdoor
    $ nano Dockerfile
    ```

    The last line should look like this:

    ```bash
    ENTRYPOINT ["/bin/bash", "-c", "source /opt/ros/humble/setup.bash && source /home/jetson/ros2_ws/install/setup.bash && ros2 launch mavrospy outdoor.py fcu_url:=/dev/ttyUSB0:921600"]
    ```

    Modify the last part to specify the `pattern`:

    ```bash
    ENTRYPOINT ["/bin/bash", "-c", "source /opt/ros/humble/setup.bash && source /home/jetson/ros2_ws/install/setup.bash && ros2 launch mavrospy outdoor.py fcu_url:=/dev/ttyUSB0:921600 pattern:=circle"]
    ```

    Rebuild via the `run_docker.sh` script:

    ```bash
    $ cd ~/mavrospy/docker/jetson/outdoor
    $ bash run_docker.sh
    ```
  </TabItem>

  <TabItem value="jetson-ros1" label="Jetson Nano (ROS1)">
    Remove the outdoor MAVROSPY container and image:

    ```bash
    $ docker container rm -f mavrospy-jetson-outdoor-container
    $ docker image rm mavrospy-jetson-outdoor
    ```

    Then modify the outdoor MAVROSPY Dockerfile:

    ```bash
    $ cd ~/mavrospy/docker/jetson/outdoor
    $ nano Dockerfile
    ```

    The last line should look like this:

    ```bash
    ENTRYPOINT ["/bin/bash", "-c", "source /opt/ros/noetic/setup.bash && source /home/rpi/catkin_ws/devel/setup.bash && roslaunch mavrospy outdoor.launch fcu_url:=/dev/ttyUSB0:921600"]
    ```

    Modify the last part to specify the `pattern`:

    ```bash
    ENTRYPOINT ["/bin/bash", "-c", "source /opt/ros/noetic/setup.bash && source /home/rpi/catkin_ws/devel/setup.bash && roslaunch mavrospy outdoor.launch fcu_url:=/dev/ttyUSB0:921600 pattern:=circle"]
    ```

    Rebuild via the `run_docker.sh` script:

    ```bash
    $ cd ~/mavrospy/docker/jetson/outdoor
    $ bash run_docker.sh
    ```
  </TabItem>
</Tabs>

</details>

Power up your drone and switch to OFFBOARD mode to watch it fly!

<div className="video_wrapper">
    <ReactPlayer className="video_player" controls height="100%" width="100%" url="https://youtu.be/EHgD98Hd8aQ"/>
</div>
